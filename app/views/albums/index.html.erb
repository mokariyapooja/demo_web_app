<h1><%= @title %></h1>
<ul>
  <% @albums.each do |album| %> 
    <h2><li>
      <%= link_to album.name, user_album_path(current_user,album) %>     
    </li> </h2>
  <% end %> 
  <h1><%= link_to 'Back', user_path(current_user) %></h1>
</ul>
   